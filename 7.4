def common_elements():
    n1 = {number for number in range(100) if number % 3 == 0}
    n2 = {number for number in range(100) if number % 5 == 0}
    # same = n1.intersection(n2)
    return n1.intersection(n2)
print(common_elements())
