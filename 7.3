def second_index(text:str,some_str:str):
    if text.count(some_str)>1:
        result = text.find(some_str,text.find(some_str) + 1)
    else:
        result = None
    return result
print(second_index('hi hi','h'))
